name: com.newrelic.consul
description: Reports status and metrics for HashiCorp Consul service. Legacy file kept for compatibility.
protocol_version: 2
os: linux

commands:
  all_data:
    command:
      - ./bin/nri-consul
    interval: 15
    prefix: config/consul
